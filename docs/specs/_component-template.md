# {{Component Name}}

> Design: {{link}} · Used on: [{{page}}](../pages/{{page}}.md) · Status: draft / in progress / done

## Purpose
What this component does and when it's used. Keep it focused — one component, one responsibility.

## Anatomy
Visual structure (regions, slots, key elements, spacing).
- ...

## Props / API
Inputs, slots, and events the component exposes.
- Props: `name: type` — description
- Slots: ...
- Emits: ...

## Implementation
Name a library primitive where one fits; flag where none does and a custom build is needed.
- ...

## Design tokens
Colors, type scale, fonts, spacing this component relies on.
- ...

## Tech used
Decisions on libraries/patterns for this component (and why, if non-obvious).
- Data fetching / state (if it fetches its own data): ...
- Other: ...

## Data
Only if the component owns its data fetching. If data is passed in via props, note
that here and leave the rest to the parent page.
- Collections / tables / endpoints & fields:
- Queries / mutations:
- Types:

## Data access / permissions
- Required policy / field-level read-write:
- Behavior when the user lacks permission (hidden / disabled / read-only):

## Client state
Server state belongs under **Data**. List here only what the server doesn't own.
- Store / local / none:

## States
One row per state. Link a design frame where there's a dedicated design; otherwise describe behavior.

| State | Design | Behavior |
| --- | --- | --- |
| Default | _link_ | |
| Loading | _link_ | |
| Empty | _link_ | |
| Error | _link_ | |

## Estimate
Rough build time, broken down by scope. Ranges, not promises.

| Scope | Estimate |
| --- | --- |
| Markup & layout | |
| Props / API & logic | |
| Data & state wiring | |
| States (loading/empty/error) | |
| Polish & responsive | |
| **Total** | |

## Tasks
- [ ] ...

## Open questions
- ...
