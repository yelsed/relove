# relove docs

Markdown documentation for `relove`, split into one page per topic and ready to feed
a simple static-site generator (plain markdown, H1 titles, relative `.md` links — no
generator-specific front-matter).

Start at **[index.md](./index.md)**.
